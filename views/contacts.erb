<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>Contact list</h2>
      <a href="/index">Home</a>
      <% @contacts.each do |contact| %>
      <p><%= contact.full_name %> <%= contact.email %> <%= contact.note %></p>
      <% end %>
    </ul>
  </body>
</html>
